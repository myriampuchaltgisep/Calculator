const add = [{ operation: 'add', value: 2 },
{ operation: 'apply', value: 3 }];
const substract = [{ operation: 'substract', value: 2 },
{ operation: 'apply', value: 3 }];
const multiply = [{ operation: 'multiply', value: 2 },
{ operation: 'apply', value: 3 }];
const divide = [{ operation: 'divide', value: 2 },
{ operation: 'apply', value: 6 }];

module.exports = {
  add,
  substract,
  multiply,
  divide,
};
